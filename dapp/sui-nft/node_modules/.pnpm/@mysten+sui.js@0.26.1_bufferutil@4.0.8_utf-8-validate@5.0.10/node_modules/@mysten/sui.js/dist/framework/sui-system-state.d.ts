import { UnserializedSignableTransaction } from '../signers/txn-data-serializers/txn-data-serializer';
import { ObjectId, SuiAddress } from '../types';
/**
 * Address of the Sui System object.
 * Always the same in every Sui network (local, devnet, testnet).
 */
export declare const SUI_SYSTEM_STATE_OBJECT_ID: string;
export declare const SUI_SYSTEM_MODULE_NAME = "sui_system";
export declare const ADD_DELEGATION_MUL_COIN_FUN_NAME = "request_add_delegation_mul_coin";
export declare const ADD_DELEGATION_LOCKED_COIN_FUN_NAME = "request_add_delegation_mul_locked_coin";
export declare const WITHDRAW_DELEGATION_FUN_NAME = "request_withdraw_delegation";
export declare const SWITCH_DELEGATION_FUN_NAME = "request_switch_delegation";
/**
 * Utility class for `0x5` object
 */
export declare class SuiSystemStateUtil {
    /**
     * Create a new transaction for delegating coins ready to be signed and executed with `signer-and-provider`.
     *
     * @param coins the coins to be used in delegation
     * @param amount the amount to delegate
     * @param gasBudget omittable only for DevInspect mode
     */
    static newRequestAddDelegationTxn(coins: ObjectId[], amount: bigint, validatorAddress: SuiAddress, gasBudget?: number, gasPayment?: ObjectId, gasPrice?: number): Promise<UnserializedSignableTransaction>;
    /**
     * Create a new transaction for withdrawing coins ready to be signed and
     * executed with `signer-and-provider`.
     *
     * @param delegation the delegation object created in the requestAddDelegation txn
     * @param stakedCoinId the coins to withdraw
     * @param gasBudget omittable only for DevInspect mode
     */
    static newRequestWithdrawlDelegationTxn(delegation: ObjectId, stakedCoinId: ObjectId, gasBudget?: number, gasPayment?: ObjectId, gasPrice?: number): Promise<UnserializedSignableTransaction>;
    /**
     * Create a new transaction for switching delegation ready to be signed and
     * executed with `signer-and-provider`.
     *
     * @param delegation the delegation object created in the requestAddDelegation txn
     * @param stakedCoinId the coins to withdraw
     * @param gasBudget omittable only for DevInspect mode
     */
    static newRequestSwitchlDelegationTxn(delegationId: ObjectId, stakedCoinId: ObjectId, newValidatorAddress: SuiAddress, gasBudget?: number, gasPayment?: ObjectId, gasPrice?: number): Promise<UnserializedSignableTransaction>;
}
